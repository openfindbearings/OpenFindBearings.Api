## OpenFindBearings.Api 领域模型设计 v1.2.0

**文档信息**

| 项目 | 内容 |
|------|------|
| 文档名称 | OpenFindBearings.Api 领域模型设计 |
| 版本 | v1.2.0 |
| 最后更新 | 2026-09-11 |

---

## 变更记录

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0.0 | 2026-06-06 | 初始版本 |
| v1.1.0 | 2026-07-06 | MerchantBearing 补充 DataSource 字段、PriceVisibility 枚举、IsOnSale/IsPendingApproval 字段；Merchant.Verify 改为带 verifiedBy 参数；LimitingSpeed 系列改为 int?；去除 PerformanceParams C/C0 校验；ContactInfo 合并 Phone 和 Mobile 为单字段 |
| v1.2.0 | 2026-09-11 | 代码审查对齐：① DataSource 字段改为 ImportedBy/ImportedAt（原 SourceSite/SourceRecordId 不存在）；② Bearing 移除 SearchCount/IncrementSearchCount（不存在），补充 ChamferRmin/Rmax/IsVerified/LastVerifiedAt/VerifiedBy/DataRemark/FavoriteCount/Category 枚举；③ Merchant 移除 SortOrder（不存在），补充 Website/Status/SuspensionReason/ProductCount/FollowerCount/ViewCount/DataSource/LastVerifiedAt/IsVerified/DataRemark，Suspend() 加 reason 参数，AddStaff 去掉 role 参数；④ User 移除 UserType/MembershipLevel（不存在），改为 Level(UserLevel 枚举)，补充大量字段和方法；⑤ ContactInfo 补充 QQ 字段和 City 计算属性；⑥ PerformanceParams 补充 HasData 字段；⑦ 实体列表补充 13 个未记录实体 |

---

### 1. 聚合根

#### 1.1 Bearing — 轴承聚合

轴承是系统的核心聚合根。每个轴承属于一个品牌和一个类型，可与多家商家关联。

| 属性组 | 字段 | 类型 |
|--------|------|------|
| 标识 | Id, CurrentCode(型号), OldNumber(曾用代号), CodeSource(代号来源) | Guid, string |
| 描述 | Description, StructureType, SizeSeries, IsStandard | string, bool |
| 尺寸 | Dimensions.InnerDiameter, OuterDiameter, Width | Dimensions 值对象 |
| 倒角 | ChamferRmin, ChamferRmax | decimal?（mm） |
| 性能 | PerformanceParams.DynamicLoad, StaticLoad, LimitingSpeed, LimitingSpeedGrease, LimitingSpeedOil | PerformanceParams 值对象（int?） |
| 其他 | Weight, PrecisionGrade, Material, SealType, CageType | decimal?, string |
| 图片 | Image3DUrl, Image2DUrl | string? |
| 来源 | DataSource.SourceType, DataSource.ImportedBy, DataSource.ImportedAt | DataSource 值对象 |
| 统计 | ViewCount | int |
| 追溯 | OriginCountry, Trademark | string? |
| 类别 | Category | BearingCategory 枚举（Domestic/Imported） |
| 数据验证 | IsVerified, LastVerifiedAt, VerifiedBy, DataRemark | bool, DateTime?, string?, string? |
| 计算 | FavoriteCount | int（计算属性） |
| 关联 | Brand(Brand), BearingType(BearingType) | 导航属性 |
| 集合 | Interchanges, MerchantBearings, FavoritedByUsers | 导航属性集合 |

**工厂方法：**
- `Bearing.Create(...)` — 创建标准轴承
- `Bearing.CreateNonStandard(...)` — 创建非标轴承

**业务方法：**
- `UpdateTechnicalSpecs(...)` — 更新技术参数
- `SetOrigin(...)` — 设置产地
- `IncrementViewCount()` — 增加查看次数

> 注：文档历史版本曾记载 `SearchCount` 和 `IncrementSearchCount()`，这两个成员实际存在于 `User` 聚合而非 `Bearing`。`Bearing` 仅有 `ViewCount`。

**唯一约束：** `(CurrentCode, BrandId)` 联合唯一索引

#### 1.2 Merchant — 商家聚合

| 属性组 | 字段 | 说明 |
|--------|------|------|
| 标识 | Id, Name, EnglishName, CompanyName | Guid, string |
| 联系 | ContactInfo(Phone, Mobile, Email, Address, ContactPerson, QQ) | ContactInfo 值对象 |
| 经营 | BusinessScope, Description, LogoUrl, Website, Type | string |
| 认证 | IsVerified, VerifiedAt, VerifiedBy | bool + DateTime? + string? |
| 等级 | Grade | MerchantGrade 枚举 |
| 状态 | Status, SuspensionReason | MerchantStatus 枚举, string? |
| 统计 | ProductCount, FollowerCount, ViewCount | int |
| 数据追溯 | DataSource, LastVerifiedAt, VerifiedBy, IsVerified, DataRemark | DataSource?, DateTime?, string?, bool, string? |
| 集合 | Staff, MerchantBearings, FollowedByUsers, LicenseVerifications | 导航属性集合 |

**状态方法：**
- `Verify(string verifiedBy)` — 认证商家（记录审核人）
- `Unverify()` — 取消认证
- `Suspend(string reason)` — 暂停（需提供原因）
- `AddStaff(User user)` — 添加员工（无 role 参数）
- `RemoveStaff(User user)` — 移除员工
- `UpdateGrade(MerchantGrade grade)` — 更新等级

> 注：文档历史版本曾记载 `SortOrder` 字段，实际代码中不存在。`Suspend()` 实际需要 `reason` 参数。`AddStaff` 实际无 `role` 参数。

#### 1.3 User — 用户聚合

与 Identity 服务的 OidcUser 通过 `AuthUserId` 桥接。

| 属性组 | 字段 | 说明 |
|--------|------|------|
| 标识 | Id, AuthUserId | Guid, Guid |
| 基本 | Nickname, Avatar, Phone, Email | string? |
| 等级 | Level, SubscriptionExpiry | UserLevel 枚举(Free/Premium/Enterprise), DateTime? |
| 注册 | RegistrationSource, RegisterIp, RegisteredAt | RegistrationSource 枚举, string?, DateTime? |
| 画像 | Occupation, CompanyName, Industry | UserOccupation?, string?, string? |
| 统计 | SearchCount, QueryCount, FirstSearchAt, LastSearchAt, LastActiveAt, LastLoginAt | int, int, DateTime?, DateTime?, DateTime?, DateTime? |
| 游客 | IsGuest, GuestSessionId, IsMerged, MergedToUserId | bool, string?, bool, Guid? |
| 商户 | MerchantId | Guid?（关联的商户 ID） |
| 集合 | Roles, FavoriteBearings, FollowedMerchants, BearingHistory, MerchantHistory, BehaviorLogs | 导航属性集合 |

**业务方法：**
- `RecordLogin()` — 记录登录
- `RecordSearch()` — 记录搜索
- `RecordQuery()` — 记录查询
- `UpdateLastActive()` — 更新最后活跃时间
- `UpgradeToPremium(DateTime expiry)` — 升级为付费用户
- `UpgradeToEnterprise(DateTime expiry)` — 升级为企业用户
- `DowngradeToFree()` — 降级为免费用户
- `MarkAsMerged(Guid targetUserId)` — 标记已合并
- `Enable()` / `Disable()` — 启用/禁用
- `AddRole(string roleName)` / `RemoveRole(string roleName)` — 角色管理
- `RecordBearingView(Guid bearingId)` — 记录轴承浏览
- `RecordMerchantView(Guid merchantId)` — 记录商家浏览
- `ClearHistory(string historyType)` — 清除浏览历史
- `AssignToMerchant(Guid merchantId)` — 关联到商户（无 role 参数）

> 注：文档历史版本曾记载 `UserType`、`MembershipLevel`(Normal/VIP/Admin)、`MigrateGuestData(...)` 方法，实际代码中不存在。用户等级使用 `Level`(UserLevel 枚举：Free/Premium/Enterprise)。`AssignToMerchant` 无 `role` 参数。

#### 1.4 用户类型说明

| 类型 | 说明 | 商户关联 |
|------|------|----------|
| Guest | 未登录游客 | 无 |
| Normal | 普通注册用户 | 无 |
| MerchantStaff | 商户员工 | 关联一个 Merchant，可管理库存 |
| MerchantAdmin | 商户管理员 | 关联一个 Merchant，可管理员工+库存 |

---

### 2. 实体

| 实体 | 表名 | 关键字段 | 说明 |
|------|------|------|------|
| Brand | brands | Code, Name, Level, Country, LogoUrl | 品牌字典（已删除 Website/Description/SortOrder） |
| BearingType | bearing_types | Code, Name, Description | 轴承类型字典（已删除 SortOrder） |
| BearingInterchange | bearing_interchanges | SourceBearingId, TargetBearingId, InterchangeType, Confidence, IsBidirectional | 替代品关系 |
| MerchantBearing | merchant_bearings | MerchantId, BearingId, PriceDescription, StockDescription, MinOrderDescription, Remarks, PriceVisibility, NumericPrice, IsOnSale, IsPendingApproval, IsFeatured, ViewCount, DataSourceType | 商家-轴承关联 |
| CorrectionRequest | correction_requests | BearingId/MerchantId, Type, Description, Status | 纠错请求 |
| AuditLog | audit_logs | UserId, Action, ResourceType, ResourceId, Details | 管理员操作日志 |
| ApiCallLog | api_call_logs | Endpoint, Method, StatusCode, Duration, UserId, IpAddress | API 调用日志 |
| SystemConfig | system_configs | Key, Value(JSON) | 系统配置键值存储 |
| LicenseVerification | license_verifications | MerchantId, ImageUrl, Status, ReviewedBy, ReviewedAt | 商家营业执照审核 |
| StaffInvitation | staff_invitations | MerchantId, Code, ExpiresAt, AcceptedAt | 员工邀请 |
| PaymentRecord | payment_records | UserId, Amount, Plan, PaidAt, ExpiresAt | 支付/订阅记录 |
| UserBearingFavorite | user_bearing_favorites | UserId, BearingId | 用户-轴承收藏关系 |
| UserBearingHistory | user_bearing_history | UserId, BearingId, ViewedAt | 用户轴承浏览历史 |
| UserMerchantFollow | user_merchant_follows | UserId, MerchantId, FollowedAt | 用户-商家关注关系 |
| UserMerchantHistory | user_merchant_history | UserId, MerchantId, ViewedAt | 用户商家浏览历史 |
| UserPreference | user_preferences | UserId, City, Province | 用户地区偏好 |
| UserBehaviorLog | user_behavior_logs | UserId, Action, Details, IpAddress | 用户行为追踪 |
| UserRole | user_roles | UserId, RoleName | 用户-角色关联 |
| Role | roles | Name, Description | 角色定义 |
| RolePermission | role_permissions | RoleName, PermissionKey, Granted | 角色-权限关联 |
| Permission | permissions | Key, Description | 权限定义 |

**MerchantBearing 关键行为：**
- `Create(merchantId, bearingId, price?, stock?)` — 创建关联（默认在售，无需审核）
- `TakeOffShelf()` — 下架
- `PutOnShelf()` — 重新上架
- `SubmitForApproval()` — 提交审核
- `Approve()` — 审核通过
- `Reject(comment)` — 审核拒绝
- `UpdateMarketInfo(price, stock, minOrder, remarks)` — 更新市场信息
- `SetPrice(description, numeric?, visibility?)` — 设置价格
- `DataSourceType` — 枚举（Crawler / Manual），区分爬虫和商户自管数据

---

### 3. 值对象

| 值对象 | 属性 | 说明 |
|--------|------|------|
| Dimensions | InnerDiameter, OuterDiameter, Width | 均为 decimal?，单位 mm |
| PerformanceParams | DynamicLoad, StaticLoad, LimitingSpeed, LimitingSpeedGrease, LimitingSpeedOil, HasData | 载荷单位 kN，转速单位 rpm（int?），HasData 表示是否有任何性能数据 |
| DataSource | SourceType(DataSourceType?), ImportedBy(string?), ImportedAt(DateTime) | Manual/Crawler/FileImport/Api/SeedData |
| ContactInfo | ContactPerson, Phone, Mobile, Email, Address, QQ, City(计算属性) | EF Core OwnsOne 映射 |

---

### 4. 基类

**BaseEntity** — 提供 `Id(Guid)`, `CreatedAt`, `UpdatedAt`, `IsActive`，以及 `AddDomainEvent()` 领域事件支持。

**ValueObject** — 基于组件值的相等性比较，不可变模式。
