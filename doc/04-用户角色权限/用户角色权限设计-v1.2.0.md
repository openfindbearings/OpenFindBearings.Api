# 用户角色权限设计

**版本：** v1.2.0  
**日期：** 2026-06-15  
**状态：** 已实施（API SeedData 完整覆盖）

---

## 变更记录

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0.0 | 2026-06-11 | 初始版本：RBAC 架构、权限键定义、角色映射、UI 交互规则 |
| v1.1.0 | 2026-06-12 | 权限键调整（合并 merchant.view、分离 sync/crawler 视图与触发）；认证集成实施中 |
| v1.2.0 | 2026-06-15 | 补充 system.manage 权限键；API SeedData 已完整覆盖全部 19 个权限键；DashboardStatsQueryHandler 从硬编码改为真实仓储计数 |

---

## 1. 架构概述

### 1.1 认证与授权分离

| 层级 | 职责 | 项目 |
|------|------|------|
| **认证（Authentication）** | OIDC 令牌签发、用户登录/登出、Token 刷新 | OpenFindBearings.Identity |
| **授权（Authorization）** | 系统级 Scope（admin/mobile/sync） | OpenFindBearings.Identity |
| **RBAC** | 细粒度角色-权限映射 | 各业务系统自行管理 |

OpenFindBearings.Identity 只负责 OIDC 认证和系统级 Scope，不管理各系统的细粒度 RBAC。每个业务系统（Admin、Sync、Crawler）自行维护角色-权限映射。

### 1.2 Admin RBAC 存储

Admin 项目拥有独立 PostgreSQL 数据库 `db_admin`，包含以下权限相关表：

| 表名 | 用途 |
|------|------|
| `admin_user_roles` | 用户-角色映射 |
| `admin_role_permissions` | 角色-权限映射（种子数据初始化） |
| `admin_audit_logs` | 操作审计日志 |
| `admin_configs` | 系统配置 |

业务数据通过各项目 API 代理访问，不直连业务数据库。

## 2. 权限键定义

### 2.1 API 端权限键（RequirePermission 字符串）

| 权限键 | 说明 | 覆盖端点 |
|--------|------|----------|
| `dashboard.view` | 查看仪表盘 | GET /api/admin/dashboard/stats |
| `bearing.view` | 查看轴承列表 | GET /api/admin/bearings, GET /api/admin/brands, GET /api/admin/bearing-types |
| `bearing.create` | 创建轴承 | POST /api/admin/bearings, POST /api/admin/brands, POST /api/admin/bearing-types |
| `bearing.edit` | 编辑轴承、恢复已删除 | PUT /api/admin/bearings/{id}, PUT /api/admin/brands/{id}, PUT /api/admin/bearing-types/{id}, PUT /api/admin/*/restore |
| `bearing.delete` | 删除轴承（软删除） | DELETE /api/admin/bearings/{id}, DELETE /api/admin/brands/{id}, DELETE /api/admin/bearing-types/{id} |
| `correction.review` | 信息纠错审核 | GET/POST /api/admin/corrections/* |
| `merchant.manage` | 商家 CRUD（创建/编辑/删除/恢复） | POST/PUT/DELETE /api/admin/merchants, PUT /api/admin/merchants/{id}/restore |
| `merchant.verify` | 商家认证审核 | POST /api/admin/merchants/{id}/verify, GET/POST /api/admin/licenses/* |
| `sync.trigger` | 触发 ETL 任务 | POST /api/sync/etl/* |
| `sync.view` | 查看同步状态 | GET /api/sync/status |
| `crawler.view` | 查看爬虫状态 | GET /api/crawlers, GET /api/crawlers/{name}/status |
| `crawler.trigger` | 触发爬虫运行 | POST /api/crawlers/{name}/run |
| `data.restore` | 恢复已删除数据 | PUT /api/admin/bearings/{id}/restore, PUT /api/admin/brands/{id}/restore, PUT /api/admin/bearing-types/{id}/restore, PUT /api/admin/merchants/{id}/restore |
| `data.harddelete` | 彻底删除（超级管理员专属） | DELETE /api/admin/bearings/{id}/hard, DELETE /api/admin/brands/{id}/hard, DELETE /api/admin/bearing-types/{id}/hard, DELETE /api/admin/merchants/{id}/hard |
| `user.manage` | 管理用户（创建/编辑/删除/恢复/重置密码） | /api/admin/users/* |
| `role.manage` | 管理角色和权限 | CRUD /api/admin/roles/*, /api/admin/permissions/* |
| `system.view` | 查看系统配置 | GET /api/admin/config |
| `system.manage` | 管理系统配置 | PUT /api/admin/config/{key}, GET /api/admin/cache/refresh-rate-limit |
| `audit.view` | 查看审计日志 | GET /api/admin/audit-logs |

### 2.2 Admin 端 PermissionKey 枚举

```csharp
public enum PermissionKey
{
    DashboardView,      // 查看仪表盘
    BearingView,        // 查看轴承列表
    BearingCreate,      // 创建轴承（品牌/类型/轴承）
    BearingEdit,        // 编辑轴承
    BearingDelete,      // 删除轴承（软删除）
    CorrectionReview,   // 信息纠错审核
    MerchantManage,     // 管理商家（创建/编辑/删除/恢复）
    MerchantVerify,     // 商家认证审核
    SyncTrigger,        // 触发 ETL 任务
    SyncView,           // 查看同步状态
    CrawlerView,        // 查看爬虫状态
    CrawlerTrigger,     // 触发爬虫运行
    DataRestore,        // 恢复已删除数据
    DataHardDelete,     // 彻底删除数据（超级管理员专属）
    UserManage,         // 管理用户
    RoleManage,         // 管理角色和权限
    SystemView,         // 查看系统配置
    AuditView           // 查看审计日志
}
```

> 枚举值用于 Admin UI 显示，API 端点使用 snake_case 字符串（如 `bearing.view`）。SeedData 中的权限键使用 snake_case 与 API 对齐。

## 3. 角色定义

### 3.1 预置角色

| 角色 | 说明 | 权限范围 |
|------|------|----------|
| **admin** | 超级管理员 | 全部权限，含恢复和彻底删除 |
| **业务员** | 日常运营 | 轴承/商家 CRUD、纠错审核、同步/爬虫查看 |
| **审计员** | 合规审查 | 仪表盘、审计日志、系统配置（只读） |

### 3.2 角色-权限映射（种子数据）

#### admin（超级管理员）

| 权限键 | 值 |
|--------|-----|
| dashboard.view | true |
| bearing.view | true |
| bearing.create | true |
| bearing.edit | true |
| bearing.delete | true |
| correction.review | true |
| merchant.manage | true |
| merchant.verify | true |
| sync.trigger | true |
| sync.view | true |
| crawler.view | true |
| crawler.trigger | true |
| data.restore | true |
| data.harddelete | true |
| user.manage | true |
| role.manage | true |
| system.view | true |
| audit.view | true |

#### 业务员

| 权限键 | 值 |
|--------|-----|
| dashboard.view | true |
| bearing.view | true |
| bearing.create | true |
| bearing.edit | true |
| bearing.delete | true |
| correction.review | true |
| merchant.manage | true |
| merchant.verify | true |
| sync.trigger | true |
| sync.view | true |
| crawler.view | true |

#### 审计员

| 权限键 | 值 |
|--------|-----|
| dashboard.view | true |
| bearing.view | true |
| sync.view | true |
| crawler.view | true |
| system.view | true |
| audit.view | true |

## 4. UI 交互与权限对应

### 4.1 删除/恢复/彻底删除

| 操作 | 业务员 | 审计员 | 超级管理员 |
|------|--------|--------|-----------|
| 删除按钮 | 可见（软删除） | 不可见 | 可见 |
| 显示已删除复选框 | 不可见 | 不可见 | 可见 |
| 恢复按钮 | 不可见 | 不可见 | 勾选"显示已删除"后可见 |
| 彻底删除按钮 | 不可见 | 不可见 | 勾选"显示已删除"后可见，confirm 二次确认 |

### 4.2 侧边栏菜单权限

| 菜单 | 所需权限 |
|------|----------|
| 仪表盘 | dashboard.view |
| 品牌列表 | bearing.view |
| 类型列表 | bearing.view |
| 轴承列表 | bearing.view |
| 商家列表 | bearing.view（隐含 merchant.manage） |
| 任务管理 | sync.view |
| 爬虫列表 | crawler.view |
| 纠错审核 | correction.review |
| 营业执照审核 | merchant.verify |
| 商家认证审核 | merchant.verify |
| 用户管理 | user.manage |
| 角色列表 | role.manage |
| 权限列表 | role.manage |
| 审计日志 | audit.view |
| 系统配置 | system.view |

## 5. 认证集成方案（实施中）

### 5.1 OAuth 2.0 授权码流程

```
用户浏览器 → Admin 登录页 → Identity /connect/authorize
    → 用户登录授权
    → 回调 Admin /Account/Callback?code=xxx
    → Admin 用 code 换 token（POST /connect/token）
    → 存储 Cookie（access_token + refresh_token）
```

### 5.2 技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| Admin 认证 | JWT Bearer + Cookie | 标准 JWT 验证，不引入 OpenIddict.AspNetCore |
| Token 交换 | 直接 HTTP 调用 Identity /connect/token | 简单直接 |
| Token 刷新 | 中间件自动检测过期 → refresh_token 刷新 | 无感刷新 |

### 5.3 Admin 配置项

```json
{
  "Identity": {
    "Authority": "https://localhost:7201",
    "ClientId": "admin_client",
    "ClientSecret": "admin-secret-key",
    "CallbackPath": "/callback"
  }
}
```

### 5.4 权限校验流程

1. 请求进入 Admin → Cookie 提取 access_token
2. JWT Bearer 验证 token 有效性
3. 查询 `admin_user_roles` 获取用户角色
4. 查询 `admin_role_permissions` 获取角色权限
5. 检查所需权限是否在用户权限列表中

## 6. 数据库 Schema

### 6.1 admin_user_roles

```sql
CREATE TABLE admin_user_roles (
    id UUID PRIMARY KEY,
    user_id UUID NOT NULL,
    role_name VARCHAR(50) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 6.2 admin_role_permissions

```sql
CREATE TABLE admin_role_permissions (
    id UUID PRIMARY KEY,
    role_name VARCHAR(50) NOT NULL,
    permission_key VARCHAR(50) NOT NULL,
    granted BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 6.3 admin_audit_logs

```sql
CREATE TABLE admin_audit_logs (
    id UUID PRIMARY KEY,
    user_id UUID,
    action VARCHAR(50) NOT NULL,
    entity_type VARCHAR(50) NOT NULL,
    entity_id UUID,
    old_value JSONB,
    new_value JSONB,
    ip_address VARCHAR(45),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 6.4 admin_configs

```sql
CREATE TABLE admin_configs (
    id UUID PRIMARY KEY,
    config_key VARCHAR(100) NOT NULL UNIQUE,
    config_value TEXT,
    description VARCHAR(500),
    updated_by UUID,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

## 7. 实现清单

### 已完成

- [x] 权限键枚举定义（PermissionKey）
- [x] 预置角色种子数据（admin/业务员/审计员）
- [x] PermissionEndpointFilter（RequirePermission 过滤器）
- [x] RoleEndpointFilter（RequireRole 过滤器）
- [x] AdminEndpoints RequirePermission 标注

### 实施中

- [ ] Identity Tenant 基础设施（Tenant 实体 + OidcUser.TenantId + 种子数据）
- [ ] Identity OIDC 授权端点（GET /connect/authorize + 登录页）
- [ ] Admin JWT Bearer 认证（Cookie + JWT Bearer 中间件）
- [ ] Admin 用户管理（调 Identity API + 本地角色）
- [ ] Admin 角色管理（admin_role_permissions CRUD）
- [ ] Admin 权限管理（PermissionKey 只读展示）
- [ ] API 清理认证代码（移除 Dev bypass + 恢复 RequireAuthorization）

### Phase 3 待实现

- [ ] 审计日志记录（每次删除/恢复/彻底删除操作）
- [ ] admin_user_roles 表种子数据（admin 用户绑定 admin 角色）
- [ ] Token 自动刷新中间件
- [ ] 多租户管理界面（当前仅 SeedData 硬编码）
