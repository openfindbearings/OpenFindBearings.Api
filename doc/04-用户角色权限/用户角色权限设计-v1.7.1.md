# 用户角色权限设计

**版本：** v1.7.1
**日期：** 2026-09-26
**状态：** 与代码同步

---

## 变更记录
| v1.7.1 | 2026-09-26 | 权限目录重排（RebalancePermissionCatalog）：37 键三段式目录——brand/type/mapping/sync.run/points.manage 拆独立键、sync.review 更名 review.sync、认证组拆 user.view/ban/manage/assign 四级（assign 含 provision，Admin 专属防提权）、permission.view 独立、merchant.import 启用；删除 app 僵尸键 correction.submit/favorite.*（app=登录态+业务资格模型）；Admin 菜单子项粒度+按钮级 PanelPerms.Has 显隐三层防线；Operator=运营+封禁（无删除/恢复/解除/分配）、Auditor=全 view 只读；PermissionKeys 死枚举类删除；用户页状态下拉（黑名单/已删除视图）+分配角色复选并存含「无」；仪表盘待办数字红色 |

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0.0 | 2026-06-11 | 初始版本：RBAC 架构、权限键定义、角色映射、UI 交互规则 |
| v1.1.0 | 2026-06-12 | 权限键调整（合并 merchant.view、分离 sync/crawler 视图与触发）；认证集成实施中 |
| v1.2.0 | 2026-06-15 | 补充 system.manage 权限键；API SeedData 已完整覆盖全部 19 个权限键；DashboardStatsQueryHandler 从硬编码改为真实仓储计数 |
| v1.3.0 | 2026-07-06 | 对齐当前代码：补充 merchant.view 权限键；Admin 端 PermissionKey 枚举补充 MerchantView/EtlManage/CrawlerManage/SystemManage；角色名与 Admin SeedData 对齐（admin/editor/viewer）；实施清单标记实际完成状态 |
| v1.4.0 | 2026-09-11 | 代码审查对齐：① API SeedData 角色改为 Admin/MerchantAdmin/MerchantStaff/Individual（原 admin/editor/viewer 不存在于 API）；② API SeedData 权限改为 19 个（移除 sync.trigger/sync.view/crawler.view/crawler.trigger，新增 correction.submit/favorite.bearing/favorite.merchant）；③ 补充 API 无硬编码 admin 用户 GUID 说明 |
| v1.5.0 | 2026-09-12 | 商户入驻权限语义落地：① MerchantAdmin/MerchantStaff 由"全局用户角色"改为"MerchantMember 成员行角色"（按 (用户,商户) 维度判定的唯一事实源）；② 记载 PermissionService.IsMerchantAdmin 恒 false 修复（改查成员表 + CurrentMerchantId）；③ 新增"当前商户上下文（X-Merchant-Id）"鉴权机制说明；④ 补充 Admin 商家审核组新增端点（approve/members）的权限覆盖；⑤ 明确平台级与商户域角色分工 |
| v1.6.0 | 2026-09-24 | 平台角色重整与面板权限接线：① Roles 种子删 MerchantAdmin/MerchantStaff 僵尸角色（商户鉴权只信 MerchantMembers 成员表），新增 Operator（操作员：日常 CRUD+审核）/Auditor（审计员：只读+审计）落地轻量三员分立，存量库由迁移 RebalancePlatformRoles 清理+补建；② 新增权限点 sync.review（同步数据审核/映射）；③ 新增 GET /api/me/permissions（角色+权限清单，Admin 登录门禁/菜单/复核数据源）与 /api/admin/users/by-auth/{sub}/roles 三端点（Admin 用户页以 Identity sub 为键分配平台角色）；④ 权限点写端点（POST/PUT/DELETE /api/admin/permissions）删除——权限点=端点过滤器代码常量，目录只读；⑤ 死代码清理：HasRole/RequireRole/RoleEndpointFilter/CanManageBearingAsync；⑥ 系统角色删除守卫名单对齐种子（Admin/Operator/Auditor/Individual）。 |

---

## 1. 架构概述

### 1.1 认证与授权分离

| 层级 | 职责 | 项目 |
|------|------|------|
| **认证（Authentication）** | OIDC 令牌签发、用户登录/登出、Token 刷新 | OpenFindBearings.Identity |
| **授权（Authorization）** | 系统级 Scope（admin/mobile/sync） | OpenFindBearings.Identity |
| **RBAC** | 细粒度角色-权限映射 | 各业务系统自行管理 |

OpenFindBearings.Identity 只负责 OIDC 认证和系统级 Scope，不管理各系统的细粒度 RBAC。每个业务系统（Admin、Sync、Crawler）自行维护角色-权限映射。

### 1.2 平台级角色 vs 商户域角色（v1.6.0）

| 维度 | 角色 | 存储 | 判定方式 |
|------|------|------|----------|
| 平台级（全局 RBAC） | Individual / Admin / SuperAdmin | `UserRoles` + `Roles` 表（API SeedData） | RequirePermission(权限键) / 全局 RBAC 查询 |
| 商户域 | MerchantAdmin / MerchantStaff | `MerchantMembers` 表（成员行 Role 字段） | **按 (用户, 商户) 成员行维度查库**，不信 JWT role claim |

> 关键变化：v1.6.0 之前 MerchantAdmin/MerchantStaff 存在全局 `UserRoles` 中，现迁移到 `MerchantMembers` 成员行。API SeedData 仍保留 MerchantAdmin/MerchantStaff 全局角色定义（存量兼容），但**业务鉴权不再以它们为依据**。

### 1.3 Admin RBAC 存储

Admin 项目拥有独立 PostgreSQL 数据库 `db_admin`，包含以下权限相关表：

| 表名 | 用途 |
|------|------|
| `admin_user_roles` | 用户-角色映射 |
| `admin_role_permissions` | 角色-权限映射（种子数据初始化） |
| `admin_audit_logs` | 操作审计日志 |
| `admin_configs` | 系统配置 |

业务数据通过各项目 API 代理访问，不直连业务数据库。

---

## 2. 权限键定义（v1.7.1 目录重排）

### 2.1 后台权限目录（37 键，三段式"资源.动作"）

> 改动说明（v1.7.1）：权限目录系统性重排——品牌/类型/映射/任务/积分从借用键拆出独立控制；
> 认证管理拆 view/ban/manage/assign 四级（封禁可下放操作员，角色分配 Admin 专属防提权）；
> `sync.review` 更名 `review.sync`；删除 app 侧僵尸键 `correction.submit`/`favorite.*`
> （**app 端权限模型=登录态+业务资格（成员表/Active/额度/封禁），不走 RBAC**，Taro 无权限键）。
> 存量库由迁移 `RebalancePermissionCatalog` 落地，与 SeedData 新装库双轨一致。

| 分组 | 权限键 | 说明 | 消费方 |
|------|--------|------|--------|
| 仪表盘 | `dashboard.view` | 查看仪表盘统计 | GET /api/admin/dashboard/stats；Admin HomeController |
| 数据-轴承 | `bearing.view` / `.create` / `.edit` / `.delete` | 轴承 CRUD | /api/admin/bearings 组端点；Data 视图按钮级 |
| 数据-品牌 | `brand.view` / `.create` / `.edit` / `.delete` | 品牌 CRUD（v1.7.1 自 bearing.* 拆出） | /api/admin/brands 组端点 |
| 数据-类型 | `type.view` / `.create` / `.edit` / `.delete` | 型号类型 CRUD（同上拆出） | /api/admin/bearing-types 组端点 |
| 数据-商家 | `merchant.view` | 商家列表（菜单粒度） | GET /api/admin/merchants |
| 数据-商家 | `merchant.manage` | 商家编辑/删除 | PUT/DELETE /api/admin/merchants |
| 数据-商家 | `merchant.verify` | 入驻审批/认证/证照审核 | verify/approve/reject/licenses 组 |
| 数据-商家 | `merchant.detach` | 强制解除商户归属 | DELETE /api/admin/merchants/{id}/detach |
| 数据-商家 | `merchant.import` | 库存导入页（菜单粒度） | Admin ImportInventory 视图 |
| 危险操作 | `data.restore` / `data.harddelete` | 恢复软删/彻底删除 | /api/admin/*/restore、/hard 组 |
| 映射维护 | `mapping.view` / `mapping.manage` | 品牌/类型映射（v1.7.1 自 sync.review 拆出） | Admin MappingController |
| 审核 | `review.sync` | 同步数据审核（原 sync.review 更名） | Admin ReviewController |
| 审核 | `correction.review` | 纠错审核 | /api/admin/corrections 组 |
| 审核 | `sourcing.view` / `sourcing.manage` | 寻货查看/下架治理 | /api/admin/sourcing 组 |
| 任务 | `sync.run` | ETL 触发+爬虫页（v1.7.1 自 dashboard.view/sync.review 拆出） | Admin SyncController/Home.Crawler |
| 认证 | `user.view` | 用户页菜单+列表+角色徽章合并读 | GET /api/admin/users/platform-roles；UsersController gate |
| 认证 | `user.ban` | 封禁/解禁（黑名单）+登录解锁+重置初始密码 | Identity enable/disable 代理（Admin action 级） |
| 认证 | `user.manage` | 新建/编辑/软删/恢复用户账号 | provision 与建号链 |
| 认证 | `user.assign` | **分配平台角色（Admin 专属——防操作员自我提权）** | /api/admin/users/by-auth/*-roles 组 |
| 认证 | `role.manage` | 角色 CRUD | /api/admin/roles 组 |
| 认证 | `permission.view` | 权限清单只读页（审计员可查） | /api/admin/permissions；Admin PermissionController |
| 系统 | `system.view` / `system.manage` | 配置读/写 | /api/admin/config 组；Config Update action 级 |
| 系统 | `points.manage` | 积分任务规则（v1.7.1 自 system.manage 拆出） | /api/admin/points/rules；PointsEndpoints |
| 审计 | `audit.view` | 审计日志 | /api/admin/audit-logs |

> 说明：`user.ban` 的封禁动作由 Admin UsersController action 级 `[PanelPermission("user.ban")]` 消费（代理 Identity enable/disable），API 侧无同名守卫；
> Admin 端点守卫与 Admin 页面策略两链共用同一键值串，键目录以 API Permissions 表为单一事实源。

### 2.2 Admin 端已废弃的枚举清单

> 改动说明（v1.7.1）：`PermissionKeys` 静态常量类（含 etl.manage/crawler.* 等死值，零消费方）整类删除；
> Admin 侧菜单/按钮判定统一走 cookie permission claims（`PanelPerms.Has(User, "键")`），权限键仅作为
> 字符串常量出现在端点守卫与视图 HasPerm 调用中，不再维护第二份枚举目录（双轨必漂移）。
## 3. 角色定义（v1.7.1 授权矩阵）

### 3.1 平台角色（Roles 表，4 个）

| 角色 | 显示名 | 语义 | 权限点 |
|------|--------|------|--------|
| Admin | 管理员 | 后台全权（UI 短路判定 panel_role=Admin 恒通过） | 全部 37 键 |
| Operator | 操作员 | 日常运营：数据维护+审核+用户封禁 | 见 3.2 |
| Auditor | 审计员 | 只读监察 | 见 3.2 |
| Individual | 普通用户 | 前端用户身份标记（app JIT 注册自动授予） | **零权限点**（v1.7.1 起，app 不走 RBAC） |

> 商户权限（MerchantAdmin/MerchantStaff）只在 MerchantMembers 成员表，永不进 Roles 表（v1.6.0 定案）。
> 一人可多角色并存（如 Individual+Operator=app 身份+后台职责），权限按并集生效。

### 3.2 Operator / Auditor 授权集（v1.7.1 重定义）

| 角色 | 权限集 |
|------|--------|
| **Operator**（21 键） | dashboard.view；bearing/brand/type 的 view+create+edit（**无 delete**）；merchant.view+manage+verify+import（**无 detach**）；mapping.view；review.sync；correction.review；sourcing.view+manage；user.view+user.ban（**无 manage/assign**） |
| **Auditor**（13 键） | 全部 view 类键（dashboard/bearing/brand/type/merchant/mapping/review.sync/correction.review/sourcing/user/permission/system/audit）.view + audit.view；零写键 |

刻意边界：删除/恢复/彻底删除/解除归属/角色分配/系统写配置/积分配置/爬虫触发全部 Admin 专属；
操作员可封禁处置但不可删号，可审核一切内容但不可删内容。

### 3.3 存量库演进

- 迁移 `RebalancePermissionCatalog`（2026-09-26）：更名+补 17 新键+Admin 补挂全量+Operator/Auditor 先清重挂+删 3 僵尸键+Individual 授权清零；幂等 NOT EXISTS 防重。
- 新装库 SeedData 目录与迁移结果一致（双轨）。

> 注：Admin 项目 `db_admin` 的 admin/editor/viewer 本地 RBAC（历史 §3.3/§3.4 所述）已于 v1.25.0 判定为僵尸系统并删除，Admin 面板权限唯一来源=API `/api/me/permissions` 的 cookie claims。
## 4. 商户域鉴权（v1.6.0 核心变更）

### 4.1 商户域角色判定

| 能力 | 判定方式 | 代码落点 |
|------|----------|----------|
| 是否为商户管理员 | 查 MerchantMember（当前用户 + CurrentMerchantId 商户维度），Role=MerchantAdmin 且 Status=Active | `PermissionService.IsMerchantAdmin()` |
| 是否为商户员工 | 查 MerchantMember（用户 + 商户维度）存在 Active 成员行 | `PermissionService.IsMerchantStaffAsync(merchantId)` |
| 是否有权操作某轴承 | 当前用户必须是该商户 Active 成员，且轴承属于该商户 | `PermissionService.CanManageBearingAsync(bearingId)` |
| 当前商户上下文 | 请求头 `X-Merchant-Id`（必须是该用户在职成员商户，否则回退缺省 = 首个 Active 成员商户） | `UserContextMiddleware` → `ICurrentUserService.CurrentMerchantId` |

### 4.2 IsMerchantAdmin 恒 false 修复

- **原缺陷**（v1.6.0 前）：`PermissionService.IsMerchantAdmin()` 读取 JWT role claim，而 JWT 的 role 来自 Identity 库（仅 SuperAdmin/Admin/User/TestUser），业务侧 MerchantAdmin 在 API 库 → 恒 false → AddStaff/RemoveStaff 管理端校验形同虚设。
- **修复**：`IsMerchantAdmin()` 改为查 `MerchantMemberRepository.GetActiveByUserAndMerchantAsync`（先决条件：已认证 + CurrentMerchantId 有效），返回成员 IsAdmin。
- 关联改造：AddStaffCommandHandler、RemoveStaffCommandHandler、SuspendMerchantMember/ActivateMerchantMember/ChangeMerchantMemberRole、四个 MerchantBearing 操作 handler（PutOnShelf/TakeOffShelf/UpdateMerchantBearing/SetPriceVisibility）均改为按成员表 + CurrentMerchantId 判定所属商户。

### 4.3 一人多商户语义

- 一个用户可通过 MerchantMember 属于多个商户，在各商户的角色相互独立（A 商户 Admin + B 商户 Staff）。
- 商户级操作必须在请求中携带/解析当前商户上下文（X-Merchant-Id）。
- 用户级数据（收藏/关注/浏览历史）与商户上下文无关，切换商户不影响。

---

## 5. UI 交互与权限对应（v1.7.1 按钮级显隐）

### 5.1 三层防线

| 层 | 机制 | 效果 |
|----|------|------|
| 菜单层 | _Layout `HasPerm(子页 view 键)` | 无权限页签不可见（品牌=brand.view、商家=merchant.view、库存导入=merchant.import、映射=mapping.view、用户=user.view……） |
| 按钮层 | 视图 `PanelPerms.Has(User, "动作键")` | 新增/编辑/删除/恢复/彻底删除/解除归属/分配角色/重置密码按钮按键显隐——**无权限者看不见按钮**，不再点了才 403 |
| API 层 | `RequirePermission(键)` | 伪造请求 403 终防线（Admin action 级 PanelPermission 与 API 守卫双链） |

### 5.2 用户页状态筛选（v1.7.1 黑名单语义）

状态下拉替代原"显示已删除"勾选：正常 / **已禁用（黑名单）** / 已锁定 / 已删除 / 全部。
Identity `UserStatusFilter` 新增 `Deleted` 枚举支持纯软删视图（v2.19.0）。
分配角色弹窗改复选可并存（普通用户+后台角色同挂），新增"无"特殊项=卸任全部角色。

### 5.3 仪表盘待办语义

第二行四张审核卡（同步数据/纠错/入驻审批/证照材料）数字红色（text-danger）= 待办提醒，
第一行存量统计不涂色。
## 6. 认证集成方案（已实施）

### 6.1 OAuth 2.0 授权码流程

```
用户浏览器 → Admin 登录页 → Identity /connect/authorize
    → 用户登录授权
    → 回调 Admin /Account/Callback?code=xxx
    → Admin 用 code 换 token（POST /connect/token）
    → BearerTokenHandler 从 cookie 提取 JWT 注入 API 请求
```

### 6.2 技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| Admin 认证 | Cookie（CookieAuthenticationDefaults） + JWT Bearer（ApiClient） | BearerTokenHandler 自动转发 JWT |
| Token 交换 | 直接 HTTP 调用 Identity /connect/token | 简单直接 |
| Token 刷新 | BearerTokenHandler 透明处理 | 无感刷新 |

### 6.3 Admin 配置项

```json
{
  "Identity": {
    "Authority": "https://localhost:7201",
    "ClientId": "admin_client",
    "ClientSecret": "admin-secret-key",
    "CallbackPath": "/callback"
  }
}
```

### 6.4 权限校验流程

1. 请求进入 Admin → Cookie 认证
2. BearerTokenHandler 从 cookie 中提取 access_token
3. API 请求携带 `Authorization: Bearer {access_token}`
4. API 端 JWT Bearer 验证 token 有效性
5. 查询 `admin_user_roles` 获取用户角色
6. 查询 `admin_role_permissions` 获取角色权限
7. PermissionEndpointFilter 检查所需权限

---

## 7. 数据库 Schema

### 7.1 admin_user_roles

```sql
CREATE TABLE admin_user_roles (
    id UUID PRIMARY KEY,
    user_id UUID NOT NULL,
    role_name VARCHAR(50) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 7.2 admin_role_permissions

```sql
CREATE TABLE admin_role_permissions (
    id UUID PRIMARY KEY,
    role_name VARCHAR(50) NOT NULL,
    permission_key VARCHAR(50) NOT NULL,
    granted BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 7.3 admin_audit_logs

```sql
CREATE TABLE admin_audit_logs (
    id UUID PRIMARY KEY,
    user_id UUID,
    action VARCHAR(50) NOT NULL,
    entity_type VARCHAR(50) NOT NULL,
    entity_id UUID,
    old_value JSONB,
    new_value JSONB,
    ip_address VARCHAR(45),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 7.4 admin_configs

```sql
CREATE TABLE admin_configs (
    id UUID PRIMARY KEY,
    config_key VARCHAR(100) NOT NULL UNIQUE,
    config_value TEXT,
    description VARCHAR(500),
    updated_by UUID,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

## 8. 实现清单

### 已完成

- [x] 权限键枚举定义（PermissionKey）
- [x] 预置角色种子数据（Admin/MerchantAdmin/MerchantStaff/Individual）
- [x] PermissionEndpointFilter（RequirePermission 过滤器）
- [x] RoleEndpointFilter（RequireRole 过滤器）
- [x] AdminEndpoints RequirePermission 标注（60 处，含 v1.6.0 新增 approve/members）
- [x] Admin JWT Bearer 认证（Cookie + BearerTokenHandler）
- [x] Admin 用户管理（调 Identity API + 本地角色）
- [x] Admin 角色管理（admin_role_permissions CRUD）
- [x] Admin 权限管理（PermissionKey 展示）
- [x] MerchantEndpoints 实现（profile/staff/bearings CRUD + members/* 成员管理 + inventory/import）
- [x] MerchantMember 成员表 + 迁移回填（代替全局角色判定商户管理员）
- [x] X-Merchant-Id 当前商户上下文鉴权
- [x] IsMerchantAdmin 恒 false 修复（改查成员表）
- [x] AddStaff/RemoveStaff/Suspend/Activate/ChangeRole 管理端校验生效
- [x] 移除 Dev Bypass（v1.0.0 → v1.1.0）
- [x] 恢复 AdminEndpoints RequireAuthorization("Admin")

### 后续优化

- [ ] Token 黑名单（登出时吊销）
- [ ] Admin 商户成员管理页（预留 merchant.member.manage 权限键）

### v1.7.1（2026-09-26）

- Individual 角色显示名定名"注册用户"（与 Admin"后台用户"对偶；中性来源描述替代层级暗示）
- 内置角色防删机制接通：DeleteRoleCommandHandler 守卫从硬编码名字名单改为读 `Role.IsSystem` 字段（SeedData 落 true 的设计初衷）；GetRolesQueryHandler 按名判断的死私法删除；Admin 分配角色弹窗消费 `isSystemRole` 显示"内置"徽章
